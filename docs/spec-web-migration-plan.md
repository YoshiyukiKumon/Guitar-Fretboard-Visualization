# ギター練習ツール Web 移植 — 方針書

| 項目 | 内容 |
|------|------|
| 対象 | [Google スプレッドシート（指板シート）](https://docs.google.com/spreadsheets/d/1sCqKepwK0TNzZmO3XilLsjShJIk2sYUvp0sV3nm0hxM/edit?gid=1029034944) |
| リポジトリ | `Guitar-Fretboard-Visualization` |
| 更新日 | 2026-05-20 |
| ステータス | 方針確定（MVP 仕様更新済み） |

---

## 0. プロジェクト運用ルール

開発・AI エージェント共通で次を守る。Cursor ルール: `.cursor/rules/project-workflow.mdc`

### 0.1 実装後の動作確認

- 実装・変更後は **動作確認まで行い、結果を報告する**。
- 確認例:
  - `npm test` / `npm run build`
  - **ブラウザ UI/UX 確認（必須）**: JavaScript の描画、CSS の適用、レイアウト・色・横スクロール・MVP 表示内容
  - `npm run dev` または `npm run preview` で画面確認
  - 自動 UI 確認: `npm run verify:ui -- http://localhost:4173`（Playwright・`scripts/verify-ui.mjs`）
- **不具合がある場合**は **修正 → 動作確認** を、解消するまで繰り返す。

### 0.2 仕様書のメンテナンス

- **仕様を調整した場合**は、**必ず本ドキュメント（`docs/spec-web-migration-plan.md`）も更新する**。
- 更新時は **§11 変更履歴** に日付と内容を追記する。
- 実装のみ先行し、仕様書が古いまま残さない。

---

## 1. 概要

### 目的

スプレッドシートで運用している「ギター練習ツール」を、**ブラウザ単体で動作する Web アプリ**に移植する。  
キー・スケール・コードに応じて指板上のインターバル（音程）を表示し、練習対象のフレット位置を視覚的に示す。

### スコープ（MVP）

| 含む | 含まない |
|------|----------|
| 指板表示（6 弦 × 24 フレット） | スケール比較（制作中ブロック） |
| キー・スケール・コード選択（master データ準拠） | スケール比較（制作中ブロック） |
| 初期値: **C / Major / △7** | |
| 指板上の **フレットマーカー（● 相当）** | 認証・サーバー保存 |
| 条件付き書式相当の色分け | チューニング変更 |
| localStorage による設定保存 | |
| PC / スマホ向けレスポンシブ UI | |

#### MVP の音楽コンテンツ（確定）

| 項目 | 値 |
|------|-----|
| キー（ルート） | **C** |
| スケール | **C Major** — 構成音: `R`, `2`, `3`, `4`, `5`, `6`, `△7` |
| コード | **C△7** — 構成音: `R`, `3`, `5`, `△7` |

※ 初期表示は C / Major / △7。選択は `localStorage` に保存する。

---

## 2. 現行スプレッドシートの構造

### 2.1 シート構成

対象は **1 シート**（`gid=1029034944`）に縦積みのブロック構成。

```
┌─────────────────────────────────────┐
│ 指板（ベース・キー C）               │
├─────────────────────────────────────┤
│ スケール（キー選択 + Altered 等）    │
├─────────────────────────────────────┤
│ コードトーン（例: A / m7-5(∅)）      │
├─────────────────────────────────────┤
│ プリセット（C Altered + Am7-5 等）   │
├─────────────────────────────────────┤
│ スケール比較 ← MVP 対象外（制作中）   │
└─────────────────────────────────────┘
```

### 2.2 データの意味

| 要素 | 意味 |
|------|------|
| 列 1〜24 | フレット番号 |
| 行 6 本 | 弦（標準チューニング）。**上＝1 弦・下＝6 弦**（演奏者から見た向き） |
| セル文字 | その位置の音が、選択キーに対するインターバル（例: `R`, `b2 / b9`） |
| 左端ラベル | 開放弦がキーに対して何度か（キー変更で変化） |
| ● 行 | 選択スケール／コードの構成音に一致するフレット位置（Web では指板上のドット模様に置換） |

### 2.3 ロジックの再現方針

グリッドの数値は **キーとチューニングから算出可能** と判断する。  
Web ではセル値の静的コピーではなく、**音楽理論エンジンによる動的生成**を採用する。

---

## 3. スプレッドシートの数式・条件付き書式について

### 3.1 読取可否

| 項目 | 読取結果 | 補足 |
|------|----------|------|
| 数式（セル参照・関数） | **CSV エクスポートでは取得不可** | エクスポートは計算済みの表示値のみ |
| 条件付き書式（色） | **HTML 公開ビューから一部推定可能** | 背景色とセルクラスの対応は解析済み |
| ● マーカー行 | **表示値として取得可能** | 理論ベースのハイライト計算と照合可能 |

### 3.2 確認した色（参考）

HTML ビューから抽出した背景色の例:

| 色（HEX） | 用途の推定 |
|-----------|------------|
| `#f4cccc` | ルート（R）の強調 |
| `#d9ead3` | スケール構成音 |
| `#6aa84f` | コードトーン／強調構成音 |
| `#d5a6bd` | コード文脈での R 等 |
| `#d0e0e3` | スケール比較ブロック等（MVP 外） |
| `#ffffff` | デフォルト（対象外の音程） |

同一インターバル表記でも **ブロックや選択状態によって色が変わる** ため、スプレッドシートの色を 1:1 で転写するより、**Web 側でルールを定義して再現**する。

### 3.3 理論ベース構築の可否

**可能。推奨する。**

実装するルールの例:

1. 標準チューニング（E-A-D-G-B-E）で各弦・各フレットの音名を求める
2. 選択キー（ルート）から半音差でインターバル ID を決定
3. インターバル ID → 表示ラベル（`b2 / b9` など併記）
4. 選択スケール／コードの構成音集合と一致するセルをハイライト・着色
5. ルート（R）は常に専用色

MVP ではスプレッドシートにない **C Major / C△7** を理論ベースで実装し、**計算結果のゴールデンテスト**で検証する（後述 §7）。

---

## 4. 要件（確定事項）

### 4.1 表記ルール

| ルール | 内容 |
|--------|------|
| 併記 | `b2 / b9` のように複数表記があるものは **併記を継続** |
| 半音・臨時記号 | スプレッドシートの `＋5`（全角）は Web では **`+5`（半角）** に統一 |
| ルート | `R` |
| 長音 | `△7`（メジャー 7th）など既存表記を維持 |

### 4.2 フレット・表示

| 項目 | 内容 |
|------|------|
| フレット数 | 24（内部データはフル保持） |
| 狭い画面 | **横スクロール**で全フレットを閲覧可能にする |
| レイアウト | PC / スマホ / タブレットそれぞれに最適化（レスポンシブ） |

### 4.3 チューニング

- **レギュラーチューニング（E-A-D-G-B-E）のみ**
- 将来拡張用にドメイン層へ `Tuning` 抽象は残してよいが、MVP UI では変更不可

### 4.4 永続化

- **localStorage** で以下を保存（想定）:
  - 最後に選択したキー
  - スケール / コードの選択
  - 表示オプション（あれば）

### 4.5 UI（スプレッドシートとの差分）

| スプレッドシート | Web アプリ |
|------------------|------------|
| フレット「間」に値が入る表形式 | **フレット線（\|）の直上**にインターバル値を表示 |
| 行＝弦、列＝フレットのセル | 弦ライン + フレット線 + 押さえ位置の背景色 |
| ● 行（スケール用） | **フレット目印インレイ**（3・5・7…）のみ。全マスには置かない |

### 4.6 インターバル表示 — 確定

- 各フレットの値 = **そのフレットを押さえたときの音程**。
- **弦（横線）とフレット位置**にカプセル型ラベルを配置する。**N フレット（N≥1）**はフレット (N-1) と N の間（当該 Grid 列の中央＝ポジションガイド●と同じ）。**開放弦**はナット列の中央。
- カプセル: 角丸最大（ピル型）。色はスプレッドシートのセル色に相当:
  - **R**: 赤背景・白文字
  - **スケール音**: 白背景・黒文字
  - **コードトーン**: やや濃い緑背景・黒文字
  - **その他**: 背景なし・半透明白文字のみ（カプセル枠なし・目立たせない）

### 4.7 フレット目印（●）— 確定

実ギターの**装飾インレイ**を表す。スケール／コードの構成音とは別。

| 項目 | 内容 |
|------|------|
| 位置 | **3, 5, 7, 9, 12, 15, 17, 19, 21** フレット区間（フレットとフレットの間）の中央、**指板（茶色エリア）の外側・下の専用行**に表示。指板と同じ CSS Grid 列に載せて配置 |
| 12 フレット | **2 つのドット**（二重目印・横並び） |
| 開放弦 | ドットなし |
| 全マス配置 | **しない** |

スケール音・コードトーンは **カプセルの背景色** で表現する（セル全面の色に相当）。

### 4.8 スタンドアロン配布 — 確定

- **Web サーバー不要**。`npm run build` で生成した **単一 HTML** をローカルで開いて動作する。
- 出力: `standalone/guitar-practice.html`（JS / CSS をインライン化）
- 利用: ファイルをダブルクリック、またはブラウザで `file://` として開く

#### 指板 UI イメージ（概念）

```
        3      5      7     12…
        |      |      |      |
        R      2      3      4   ← ラベルは | の上
E ————[====]—|—[====]—|—[====]—|—
        ●      ●      ●      ●   ← インレイ（指板下部・3,5,7…のみ）
```

- フレット番号: 各フレット線の上
- 弦名: 左端固定
- モバイル: フレット領域を横スクロール

---

## 5. 技術方針

### 5.1 アーキテクチャ

```
┌─────────────────────────────────────────┐
│  UI（キー / スケール / コード / 指板）    │
├─────────────────────────────────────────┤
│  Application（状態・localStorage）       │
├─────────────────────────────────────────┤
│  Domain（音程・スケール・コード・指板計算）│
└─────────────────────────────────────────┘
```

- **バックエンド不要**
- ビルド: Vite + TypeScript + `vite-plugin-singlefile`（単一 HTML）
- **主な配布形態**: `standalone/guitar-practice.html` をローカルで開く（サーバー不要）
- 任意: GitHub Pages 等への静的ホスティングも可（`dist/index.html`）

### 5.2 ディレクトリ構成（予定）

```
guitar-practice/
├── docs/                    # 本ドキュメント群
├── src/
│   ├── domain/              # 音楽理論・指板計算
│   ├── ui/                  # 指板・コントロール
│   ├── app/                 # 状態・永続化
│   └── main.ts
├── tests/                   # ゴールデンテスト（指板・ハイライト）
└── package.json
```

### 5.3 ドメインモデル（案）

```typescript
type IntervalId =
  | 'R' | 'b2' | '2' | 'm3' | '3' | '4' | 'b5' | '5' | 'sharp5'
  | '6' | 'b7' | 'maj7';

interface IntervalDisplay {
  id: IntervalId;
  label: string;  // 例: "b2 / b9"
}

interface ScaleDef {
  id: string;
  name: string;
  intervals: IntervalId[];
}

interface ChordDef {
  id: string;
  name: string;
  intervals: IntervalId[];
}

interface FretCell {
  string: number;       // 0..5
  fret: number;         // 0=open, 1..24
  interval: IntervalId;
  label: string;
  inScale: boolean;
  inChord: boolean;
  isRoot: boolean;
}
```

### 5.4 色・ハイライトルール（Web 定義）

MVP で採用する CSS 変数例（スプレッドシートに近い配色）:

| トークン | 用途 | 参考 HEX |
|----------|------|----------|
| `--color-root` | ルート（R） | `#f4cccc` |
| `--color-scale-tone` | スケール構成音 | `#d9ead3` |
| `--color-chord-tone` | コード構成音 | `#6aa84f` |
| `--color-marker-scale` | スケール音ドット | `#d9ead3`（塗り） |
| `--color-marker-chord` | コードトーンドット | `#6aa84f`（塗り） |
| `--color-marker-root` | ルートドット | `#f4cccc` または chord 色 + 枠 |
| `--color-default` | その他 | `#ffffff` / 透明 |

※ 最終色は実装時にスプレッドシートと並べて微調整する。

---

## 6. 実装フェーズ

### Phase 1 — MVP（本プロジェクトのゴール）

1. ドメイン: チューニング・キー C・インターバル計算
2. 定義データ: **C Major** スケール、**C△7** コード
3. 指板コンポーネント（フレット上ラベル・弦上ドット・横スクロール）
4. 色分け（セル背景またはラベル背景）+ フレットマーカー
5. localStorage（将来の選択肢用に最小限。MVP は C 固定のためほぼ表示設定のみ）
6. レスポンシブ CSS
7. ゴールデンテスト（C Major / C△7 のマーカー位置・ラベル）

### Phase 2 — 4 ビュー切り替え（実装済み）

スプレッドシートの縦積みブロックに相当する **4 種類の指板ビュー**をタブで切り替える。

| ビュー | 強調 | その他 |
|--------|------|--------|
| 指板ビュー | ルート（R）のみ赤 | ルート以外は白カプセル（スケールビューと同スタイル） |
| スケールビュー | ルート（R）は赤、それ以外のスケール構成音 | 一律「その他」 |
| コードトーンビュー | ルート（R）は赤、それ以外のコード構成音 | 一律「その他」 |
| 複合ビュー | スケールルート（赤）> コードルート（オレンジ）> コード > スケール | 同一音で両ルートのときはスケールルート色 |

- 選択状態は `localStorage`（`viewMode`）に保存する。**初期値はスケール**（`viewMode: scale`）。
- **指板表示**: ラベル **指板** の横で **●（ドット） / インターバル / 音名 / カナ** を切り替え（`labelMode` を localStorage に保存）。**初期値は ●（ドット）**。**カナ** タブは画面上 **音名(カナ)** の略称（`aria-label` で全称）。固定ドの **ドレミファソラシ** 表記（`C` → `ド`, `C#` → `ド♯` 等。スペルは音名モードと同じ）。**音量** はスピーカーアイコン＋短いスライダー（数値 % は非表示、`aria-valuetext` で読み上げ）。**●** モードは他表示と同じ色の**円形カプセル**のみ（中央の文字なし・その他のマスは非表示）。**インターバル**は通常スケールルート基準。**コード**表示モードのときのみコードルート基準（例: スケール C・コード A で 6 弦開放 E はスケールで `3`、コードで `5`）。音名はスケールルートに応じた表記（ダイアトニック音度＋キーに合う異名同音。例: F# キーで C→**B#**、Bb キーで B→**Cb**、Eb/Ab/Bb キーはフラット優先）。
- **音声再生**: 構成音パネル — スケールは「▶ 再生」で 1 オクターブを **4 分音符 1 音ずつ**順次再生（再生中は **■ 停止**）。コードは **▶ 同時**（全トーン同時）、**▶ アルペジオ**（ルートから半音順、再生中 **■ 停止**）、**∞ リピート**（4/4・4 分音符 4 拍のパターンを **■ 停止までループ**）の 3 ボタン（Web Audio API・ファイル不要）。**テンポ**は構成音セクション右上の **BPM**（40〜240、localStorage 保存）で制御。**ギター系**（アコースティック・エレキ）の **▶ 同時** / **▶ リピート** はルートから昇順に **約 32ms ずつ** 開始をずらしてストローク風に再生。**ピアノ・シンセ**は従来どおり完全同時。コードの **9 度以上**（コード名に 9・11・13 等がある併記ラベル）は 1 オクターブ上で鳴らす。**9 未満**（sus4・6・add9 の 2 側など）は従来どおり 1 オクターブ内。**スケール・コード再生**は **C〜F# ルートで 1 オクターブ下**、**G 以上で 2 オクターブ下**。指板タップは従来どおり。指板の **フレット間タップ領域**（弦×フレット交点、フレット線・隣弦から約 10% / 18% マージン）で実音を単音再生（表示モードに関係なく全交点がタップ可）。**交点本体は `pointer-events: none`**。**横スクロールは指板全体で統一**（ホイール・ドラッグ。ドラッグ直後のタップは再生しない）。
- **音量**: スライダーで 0〜100% 調整（`volume` を localStorage に保存）。
- **キー・スケール・コード選択 UI**（`scaleKeyId` / `scaleId` / `chordKeyId` / `chordId` を localStorage に保存）。**スケールのルートとコードのルートは別指定**。

### Phase 2+（将来）

- **ダイアトニックコード一覧**（画面最下部）— 仕様: [spec-diatonic-chords.md](./spec-diatonic-chords.md)
- スケール比較 UI
- 追加スケール・コード（master 更新時）
- フレット範囲の絞り込み
- PWA / オフライン

---

## 7. 検証・受け入れ基準（MVP）

### 7.1 受け入れ基準

| # | 基準 |
|---|------|
| 1 | キー C + C Major の構成音位置に **弦上ドット** が表示される |
| 2 | C△7 の構成音（R, 3, 5, △7）位置は、スケールドットより **強いスタイル** で表示される |
| 3 | C Major のラベルが `R`, `2`, `3`, `4`, `5`, `6`, `△7` で表示される |
| 4 | 併記ルール（`b2 / b9` 等）はデータ定義に含め、将来スケール追加時に利用可能 |
| 5 | `+5` は半角表記とする |
| 6 | スマホ幅で横スクロールし 24 フレットすべて閲覧できる |
| 7 | インターバルが **フレット間（押さえ位置）** にカプセル表示される（開放弦は弦名とナットの間） |
| 8 | フレット目印は **3,5,7,9,12(2),15,17,19,21** のみ（全マスにドットなし） |
| 9 | `standalone/guitar-practice.html` が **file:// で起動**できる |
| 8 | localStorage が利用可能（MVP では UI 設定の保存で可） |

### 7.2 ゴールデンテスト参照データ（理論算出）

標準チューニング・キー C における **フレット 1〜24** のマーカー対象（抜粋）。

**C Major — 弦ごとのフレット番号（スケール音）**

| 弦 | フレット（1〜24 のうち） |
|----|--------------------------|
| 6 (E) | 1, 3, 5, 7, 8, 10, 12, 13, 15, 17, 19, 20, 22, 24 |
| 5 (A) | 2, 3, 5, 7, 8, 10, 12, 14, 15, 17, 19, 20, 22, 24 |
| 4 (D) | 2, 3, 5, 7, 9, 10, 12, 14, 15, 17, 19, 21, 22, 24 |
| 3 (G) | 2, 4, 5, 7, 9, 10, 12, 14, 16, 17, 19, 21, 22, 24 |
| 2 (B) | 1, 3, 5, 6, 8, 10, 12, 13, 15, 17, 18, 20, 22, 24 |
| 1 (E) | 1, 3, 5, 7, 8, 10, 12, 13, 15, 17, 19, 20, 22, 24 |

**C△7 — 弦ごとのフレット番号（コードトーン）**

| 弦 | フレット |
|----|----------|
| 6 (E) | 3, 7, 8, 12, 15, 19, 20, 24 |
| 5 (A) | 2, 3, 7, 10, 14, 15, 19, 22 |
| 4 (D) | 2, 5, 9, 10, 14, 17, 21, 22 |
| 3 (G) | 4, 5, 9, 12, 16, 17, 21, 24 |
| 2 (B) | 1, 5, 8, 12, 13, 17, 20, 24 |
| 1 (E) | 3, 7, 8, 12, 15, 19, 20, 24 |

※ **開放弦（フレット 0）にはドットを付けない**（ラベルのみ表示可）。

---

## 8. master シート（マスターデータ）

### 8.1 読取可否

**読取可能。** `sheet=master` の CSV エクスポートで取得済み（2026-05-20 時点）。

| シート名 | gid（参考） | 役割 |
|----------|-------------|------|
| `master` | `1143285009` | 計算根拠・スケール／コード定義・指板マトリクス |
| `scale`（指板） | `1029034944` | UI プロトタイプ（表示・色・● 行） |
| `シート2` | `505244506` | （未解析・MVP 外） |

Web アプリの **データ定義の正** は `master` シートとする。`scale` シートは UI 参考。

### 8.2 master シートの構成

```
┌──────────────────────────────────────────────┐
│ 指板マトリクス（弦 1〜6 × フレット 0〜24）    │  ← 各セル = 音程クラス 0〜11（半音単位）
├──────────────────────────────────────────────┤
│ 調音（キー名 ↔ 半音 index 0〜11）            │
├──────────────────────────────────────────────┤
│ スケール一覧（名称 + 構成音ラベル列）         │
├──────────────────────────────────────────────┤
│ コード一覧（名称 + 構成音ラベル列）           │
└──────────────────────────────────────────────┘
```

### 8.3 計算根拠（スプレッドシート設計の再現）

実装は次のパイプラインに従う。

```
1. master の指板マトリクスから (弦, フレット) → 音程クラス pc (0〜11) を取得
2. 選択キーの root_pc（調音テーブル）を取得
3. 相対半音: rel = (pc - root_pc + 12) % 12
4. rel → 表示ラベル（例: 2 → "2 / 9", 11 → "△7"）※ §4.1 併記ルール
5. 選択スケール／コードのラベル集合とセルラベルを照合
6. 一致 → 指板上にドット（フレット 1〜24 のみ。開放弦はドットなし）
```

**指板マトリクスの意味**

- キーに依存しない **絶対音程クラス（12 音）** を格納
- 各弦は開放弦の音程クラスから半音ずつ増加（24 フレットで 2 周）
- 例（6 弦 E）: 開放=4, 1F=5, 2F=6, … 11 → 0 → 1 …

**調音テーブル（キー）**

| キー | index |
|------|-------|
| C | 0 |
| C# / Db | 1 |
| D | 2 |
| Eb | 3 |
| E | 4 |
| F | 5 |
| F# / Gb | 6 |
| G | 7 |
| Ab | 8 |
| A | 9 |
| Bb | 10 |
| B | 11 |

### 8.4 スケール定義（master より・Phase 2 で順次対応）

| スケール名 | 構成音ラベル |
|------------|----------------|
| Major | R, 2 / 9, 3, 4 / 11, 5, 6 / 13, △7 |
| Natural Minor | R, 2 / 9, m3 / #9, 4 / 11, 5, +5 / b13, b7 |
| Harmonic Minor | R, 2 / 9, m3 / #9, 4 / 11, 5, +5 / b13, △7 |
| Melodic Minor | R, 2 / 9, m3 / #9, 4 / 11, 5, 6 / 13, △7 |
| Major Penta Tonics | R, 2 / 9, 3, 5, 6 / 13 |
| Minor Penta Tonics | R, m3 / #9, 4 / 11, 5, b7 |
| Altered | R, b2 / b9, m3 / #9, 3, b5 / #11, +5 / b13, b7 |
| HP5 | R, b2 / b9, 3, 4 / 11, 5, +5 / b13, b7 |
| Lydian b7 | R, 2 / 9, 3, b5 / #11, 5, 6 / 13, b7 |
| Dorian | R, 2 / 9, m3 / #9, 4 / 11, 5, 6 / 13, b7 |
| Phrygian | R, b2 / b9, m3 / #9, 4 / 11, 5, +5 / b13, b7 |
| Lydian | R, 2 / 9, 3, b5 / #11, 5, 6 / 13, △7 |
| Mixolydian | R, 2 / 9, 3, 4 / 11, 5, 6 / 13, b7 |
| Locrian | R, b2 / b9, m3 / #9, 4 / 11, b5 / #11, +5 / b13, b7 |
| Whole/Half Diminish | R, 2 / 9, m3 / #9, 4 / 11, b5 / #11, +5 / b13, 6 / 13, △7 |
| Half/Whole Diminish | R, b2 / b9, m3 / #9, 3, b5 / #11, 5, 6 / 13, b7 |
| Whole Tone | R, 2 / 9, 3, b5 / #11, +5 / b13, b7 |
| KUMOI Penta Tonics | R, 2 / 9, m3 / #9, 5, 6 / 13 |
| Diminished 6th | R, 2 / 9, 3, 4 / 11, 5, +5 / b13, 6 / 13, △7 |

### 8.5 コード定義（master より・Phase 2 で順次対応）

| コード名 | 構成音ラベル |
|----------|----------------|
| major triad | R, 3, 5 |
| m | R, m3 / #9, 5 |
| m7 | R, m3 / #9, 5, b7 |
| △7 | R, 3, 5, △7 |
| 7 | R, 3, 5, b7 |
| m7-5 (∅) | R, m3 / #9, b5 / #11, b7 |
| m△7 | R, m3 / #9, 5, △7 |
| △7(9) | R, 3, 5, △7, 2 / 9 |
| △7(#11) | R, 3, 5, △7, b5 / #11 |
| 6(9) | R, 3, 6 / 13, 2 / 9 |
| 6 | R, 3, 5, 6 / 13 |
| aug | R, 3, +5 / b13 |
| 7+5 | R, 3, +5 / b13, b7 |
| △7(+5) | R, 3, +5 / b13, △7 |
| dim | R, m3 / #9, b5 / #11 |
| dim7 | R, m3 / #9, b5 / #11, 6 / 13 |
| sus4 | R, 4 / 11, 5 |
| sus2 | R, 2 / 9, 5 |
| power | R, 5 |
| add9 | R, 3, 5, 2 / 9 |
| 7(9) | R, 3, 5, b7, 2 / 9 |
| 7(b9) | R, 3, 5, b7, b9 |
| 7(#9) | R, 3, 5, b7, m3 / #9 |
| 7(11) | R, 3, 5, b7, 4 / 11 |
| 7(9, 11) | R, 3, 5, b7, 2 / 9, 4 / 11 |
| 7(#11) | R, 3, 5, b7, b5 / #11 |
| 7(13) | R, 3, 5, b7, 6 / 13 |
| 7(b13) | R, 3, 5, b7, +5 / b13 |
| m6 | R, m3 / #9, 5, 6 / 13 |
| m9 | R, 3, b7, 2 / 9 |
| m11 | R, 3, b7, 4 / 11 |

※ `master` 内に `6` が 2 行ある（重複定義）。実装時は 1 定義に正規化する。

### 8.6 MVP と master の対応

| MVP | master での行 |
|-----|----------------|
| C Major | 調音 `C`(0) + スケール `Major` |
| C△7 | 調音 `C`(0) + コード `△7` |

Phase 1 では上記 2 組のみ UI に接続する。§8.4・§8.5 の全件は **JSON / TypeScript のマスターデータ** として `src/domain/data/` に移植する（MVP 後に選択 UI を追加）。

---

## 9. 未決定・確認事項

| 項目 | 状態 | メモ |
|------|------|------|
| 開放弦へのドット表示 | **確定: なし** | フレット 0 はラベルのみ |
| キー選択 UI | Phase 2 | MVP は C 固定。調音テーブルは master 準拠で実装 |
| `rel` → 表示ラベル変換表 | 要実装 | master のラベル文字列をそのまま使用（併記維持） |
| 公開 URL・ドメイン | 未着手 | 静的ホスティングで対応 |

---

## 10. 参考リンク

- 元スプレッドシート: https://docs.google.com/spreadsheets/d/1sCqKepwK0TNzZmO3XilLsjShJIk2sYUvp0sV3nm0hxM/edit?gid=1029034944

---

## 11. 変更履歴

| バージョン | 日付 | 内容 |
|------------|------|------|
| 0.1 | 2026-05-20 | 初版。MVP 範囲・表記・UI・色・localStorage を確定 |
| 0.2 | 2026-05-20 | MVP を C Major / C△7 に変更。● を指板上のドット模様に確定。ゴールデンテストデータ追加 |
| 0.3 | 2026-05-20 | 開放弦ドットなし確定。master シート読取・計算根拠・スケール／コード一覧を追記 |
| 0.4 | 2026-05-20 | §0 プロジェクト運用ルール（動作確認・仕様書メンテナンス）を追加 |
| 0.5 | 2026-05-20 | §0 に UI/UX（JS/CSS 適用）のブラウザ確認を必須化 |
| 0.6 | 2026-05-20 | スタンドアロン HTML・フレット目印インレイ・ラベルをフレット線上に変更 |
| 0.7 | 2026-05-20 | 弦×フレット交点のカプセル表示・色分け（サンプル画像準拠） |
| 0.8 | 2026-05-20 | 指板背景を濃い茶色（木目風グラデーション）に変更 |
| 0.9 | 2026-05-20 | 開放弦ラベルを指板左端（弦名＋カプセル）に配置 |
| 1.0 | 2026-05-20 | 開放弦をナット交点に復帰・フレット番号をフレット線上に配置 |
| 1.1 | 2026-05-20 | フレット目印を指板下部に移動・弦間余白を約半分に調整 |
| 1.2 | 2026-05-20 | フレット目印を指板外側（下）に配置・ドットサイズ約 80% |
| 1.3 | 2026-05-20 | 「その他」インターバルを背景なしの半透明白文字表示に変更 |
| 2.0 | 2026-05-20 | Phase 2: 指板／スケール／コードトーン／複合の 4 ビュー切り替え |
| 2.1 | 2026-05-20 | スケール／コードトーンビューでもルート（R）を赤表示 |
| 2.2 | 2026-05-20 | スケールトーンのカプセル背景を白に変更 |
| 2.3 | 2026-05-20 | 凡例「その他」のサンプル表記を中立表示（—）に変更 |
| 2.4 | 2026-05-20 | 指板ビューのルート以外を白カプセル表示に変更 |
| 2.5 | 2026-05-20 | 弦の表示順を上＝1 弦・下＝6 弦に変更 |
| 2.6 | 2026-05-20 | インターバル／音名のラベル表示トグルを追加 |
| 2.7 | 2026-05-20 | キー・スケール・コード選択 UI を追加（localStorage 保存） |
| 2.8 | 2026-05-20 | スケールのルートとコードのルートを別々に指定可能に |
| 2.9 | 2026-05-20 | スケール順次再生・コード同時再生（Web Audio API） |
| 2.10 | 2026-05-20 | 再生音量スライダー（0〜100%、localStorage 保存） |
| 2.11 | 2026-05-20 | 構成音パネルにスケール名・コード名と音名行を追加 |
| 2.12 | 2026-05-20 | 狭幅時の選択 UI をスケール行・コード行の 2 行レイアウトに変更 |
| 2.13 | 2026-05-20 | GitHub Actions による GitHub Pages 自動デプロイを追加 |
| 2.14 | 2026-05-20 | Pages ワークフローを Actions v5（Node 24）に更新、ロールバック手順を追加 |
| 2.15 | 2026-05-20 | 指板カプセルのタップ／クリックで実音を再生 |
| 2.16 | 2026-05-20 | カプセルのタップ領域を表示位置と一致（幅 0 アンカー・transform 不使用） |
| 2.17 | 2026-05-20 | 隣フレット交点のクリック遮蔽を解消（交点 pointer-events: none） |
| 2.18 | 2026-05-20 | スケールルート Eb/Ab/Bb をフラット表記、音名もフラット優先 |
| 2.19 | 2026-05-20 | 音名をキー文脈の異名同音表記に対応（B# / Cb 等） |
| 2.20 | 2026-05-20 | フレット目印をフレット間に配置、表示切替ラベル短縮、ラベルと音量を1行に |
| 2.21 | 2026-05-20 | フレット目印を絶対座標でフレット間に再配置、凡例から目印を削除 |
| 2.22 | 2026-05-20 | フレット目印をフレット区間中央（N-0.5 列）に修正 |
| 2.23 | 2026-05-20 | フレット目印を指板と同じ Grid 列の中央（grid-column）に変更 |
| 2.24 | 2026-05-20 | 音名表記をスケールルートの調号で統一、コードルートプルダウンを動的表記に（`docs/spec-note-spelling-rules.md`） |
| 2.25 | 2026-05-20 | カスタムスケール・コードライブラリ（練習/ライブラリ二モード、CSV、リセット）— `docs/spec-custom-music-library.md` |
| 3.0 | 2026-05-23 | 設定タブ・再生楽器選択（7種）— `docs/spec-settings.md` |
| 2.26 | 2026-05-20 | ライブラリ UI: ID 自動発行・構成音プルダウン（仕様 v1.1） |
| 2.27 | 2026-05-20 | 指板カプセルをフレット区間の列中央に配置（ポジションガイド●と同軸・開放弦はナット列中央） |
| 2.28 | 2026-05-20 | 0 フレット左側（弦名・ナット列）の余白拡張、開放弦カプセルの最大幅をナット列に合わせる |
| 2.29 | 2026-05-20 | コード再生で 9 度以上を 1 オクターブ上に（`chord-playback.ts`） |
| 2.30 | 2026-05-20 | コード再生にアルペジオ（▶ 同時 / ▶ アルペジオ の 2 ボタン、R から昇順） |
| 2.31 | 2026-05-20 | ライブラリ編集フォームにプレビュー再生を追加 |
| 2.32 | 2026-05-20 | スケールルート（赤）とコードルート（オレンジ）を色分け |
| 2.33 | 2026-05-20 | 複合ビューで両ルート一致時はスケールルート色を優先 |
| 2.34 | 2026-05-20 | 指板ラベル追加、● ドット表示モード（円形カプセルのみ・色は共通） |
| 2.35 | 2026-05-20 | 指板表示の初期値・ボタン順を ● / インターバル / 音名 に変更 |
| 2.36 | 2026-05-20 | 表示モードの初期値をスケールに変更 |
| 2.37 | 2026-05-20 | コード表示モードのインターバル表記をコードルート基準に変更 |
| 2.38 | 2026-05-20 | ダイアトニックコード表示の開発方針ドキュメント追加（`spec-diatonic-chords.md`） |
| 2.39 | 2026-05-20 | ダイアトニックコード仕様 v1.1（8 音以上・四和音・タップ再生） |
| 2.40 | 2026-05-20 | ダイアトニックコード仕様 v1.2（相対/実 2 段表示・列レイアウト） |
| 2.41 | 2026-05-20 | ダイアトニックコード仕様 v1.3（相対表記の ♯/♭ 度数 prefix） |
| 2.42 | 2026-05-20 | ダイアトニックコード UI：列タップを廃止し再生・選択ボタンに分離 |
| 2.43 | 2026-05-20 | 半全減・全半減スケールマスタを 8 音に修正（`b7` / `△7`） |
| 2.44 | 2026-05-20 | コードマスタに `△7(+5)`（`maj7-sharp5`）追加 |
| 2.45 | 2026-05-20 | 組み込みスケール `diminished-6th`（Diminished 6th・8 音）を追加 |
| 2.46 | 2026-05-20 | power 表示名を `power` に短縮。E 以上ルートのスケール/コード再生を 1 オクターブ下げ |
| 2.47 | 2026-05-20 | ギター系コード同時再生をルート昇順ストローク（約 32ms 間隔）に変更 |
| 2.48 | 2026-05-20 | 指板タップ領域をフレット間全体に拡張、ダイアトニックセル枠全体を再生化 |
| 2.49 | 2026-05-20 | スケール/コード再生を全ルートで 1 オクターブ下げ |
| 2.50 | 2026-05-20 | E 以上ルートのスケール/コード再生を合計 2 オクターブ下げに変更 |
| 2.51 | 2026-05-20 | オクターブ下げ境界を C〜G（-12）/ A 以上（-24）に変更 |
| 2.52 | 2026-05-20 | オクターブ下げ境界を C〜F#（-12）/ G 以上（-24）に変更 |
| 2.53 | 2026-05-20 | 指板タップ領域拡張後の横スクロール不能を修正（交点 pointer-events・pan-x） |
| 2.54 | 2026-05-20 | 指板スクロールを領域全体で統一（ホイール・ドラッグ、タップ誤発火抑制） |
| 2.55 | 2026-05-20 | GitHub リポジトリ名を `Guitar-Fretboard-Visualization` に修正（綴り） |
| 2.56 | 2026-05-24 | 構成音 BPM・再生停止・コード 4/4 リピート再生 |
| 2.57 | 2026-05-30 | コードリピートを ■ 停止までループ再生に変更 |
| 2.58 | 2026-05-30 | リピートボタンを ∞ 表示、ダイアトニック列にも ∞ リピート追加 |
| 2.59 | 2026-05-30 | ソングモード（コード進行・伴奏）仕様 v1.0 — `docs/spec-song-mode.md` |
| 2.60 | 2026-05-30 | ソングモード仕様 v1.1（曲キー継承・繰り返し記号・playCount） |
| 2.61 | 2026-05-30 | ソングモード仕様 v1.2（コーダ・D.C./D.S. al Coda を v1 スコープに） |
| 2.62 | 2026-05-30 | ソングモード仕様 v1.3（ブロックマスター・曲 parts 構成） |
| 2.63 | 2026-05-30 | ソング CSV 入出力 v1.4（library CSV に統合） |
